pipeline {
    agent any

    stages {
    stage('clone')
    {
    steps{
    git 'https://github.com/serpantdi/cal.git'
    }
    }

        stage('build')
        {
            steps{
                bat 'mvn clean compile'
                bat 'mvn clean install'
            }
        }
    }
}
